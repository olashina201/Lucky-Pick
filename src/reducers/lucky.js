export default (lucky = [], action) => {
    switch (action.type) {
        case 'FETCH_ALL':
            return lucky;
        case 'CREATE':
            return lucky;
        default:
            returnlucky
    }
}